# Stripe publishable key (client-side)
REACT_APP_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key

# Leave empty for local dev (CRA proxy handles /api)
# Set to your Vercel URL in production only if API is hosted separately
REACT_APP_API_URL=
